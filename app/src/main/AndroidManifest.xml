<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="idat.com.administracion.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdministracionDeEdificiosApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.HistorialActivity"></activity>
        <activity android:name=".activities.ListaReservaActivity" />
        <activity android:name=".activities.NosotrosActivity" />
        <activity android:name=".activities.MiDepartamento_Activity" />
        <activity android:name=".activities.RegistrarUsuarioActivity" />
        <activity android:name=".activities.RegistroSolicitudActivity" />
        <activity android:name=".activities.PasarelaPagosActivity" />
        <activity android:name=".activities.HogarActivity" />
        <activity android:name=".activities.ListaInvitadosActivity" />
        <activity android:name=".activities.DecisionActivity" />
        <activity android:name=".activities.LoginActivityMysql" />
        <activity android:name=".activities.Detalle_Departamento" />
        <activity android:name=".activities.Depa_Disponible_Activity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegistroActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AdministracionDeEdificiosApp.NoActionBar" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.LoginActivity" />
    </application>

</manifest>